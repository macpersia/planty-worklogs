#!/bin/bash
# The logic to start up your application should be put in this
# script. The application will work only if it binds to

# Changed by Hadi
## $OPENSHIFT_DIY_IP:8080
#nohup $OPENSHIFT_REPO_DIR/diy/testrubyserver.rb $OPENSHIFT_DIY_IP $OPENSHIFT_REPO_DIR/diy |& /usr/bin/logshifter -tag diy &

export JAVA_HOME=/usr/lib/jvm/java-1.8.0
export PATH="$JAVA_HOME/bin:$PATH"

export JAVA_OPTS="-Dhttp.address=$OPENSHIFT_DIY_IP -Dhttp.port=$OPENSHIFT_DIY_PORT"
export JAVA_OPTS="$JAVA_OPTS -Xms256m -Xmx416m -XX:MetaspaceSize=64m -XX:MaxMetaspaceSize=96m -Duser.home=$OPENSHIFT_DEPENDENCIES_DIR/fakehome"
export JAVA_OPTS="$JAVA_OPTS -Dmongodb.host=$OPENSHIFT_MONGODB_DB_HOST -Dmongodb.username=$OPENSHIFT_MONGODB_DB_USERNAME -Dmongodb.password=$OPENSHIFT_MONGODB_DB_PASSWORD"
# Later commented by Hadi
#nohup $OPENSHIFT_REPO_DIR/planty-jira-web/target/universal/stage/bin/planty-jira-web |& /usr/bin/logshifter -tag diy &

# Temporaily for staging
cd $OPENSHIFT_REPO_DIR
nohup $OPENSHIFT_BUILD_DEPENDENCIES_DIR/sbt/bin/sbt -v -mem 416 -Dsbt.log.noformat=true -Duser.home=$OPENSHIFT_DEPENDENCIES_DIR/fakehome jiraWeb/run $JAVA_OPTS |& /usr/bin/logshifter -tag diy &
